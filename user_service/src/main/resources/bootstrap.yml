server:
  port: ${port:9009}


spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db8
    username: root
    password: root
  application:
    name: user-service

mybatis:
  type-aliases-package: com.sugeladi.pojo

eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka
